using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class FillIndex : MonoBehaviour
{
    public TextMeshProUGUI Text;
    // public ContentIndex contextScript;

    void Update()
    {
        // int currentIndex = contextScript.indexContent + 1;
        // int maxIndex = contextScript.indexContentMax + 1;
        // Text.text = currentIndex.ToString("D2") + "/" + maxIndex.ToString("D2");
    }
}
